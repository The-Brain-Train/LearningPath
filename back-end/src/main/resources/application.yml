# default connection pool
debug: true
spring:
  data:
    mongodb:
      uri: mongodb://root:example@localhost:27017/
      database: roadMap
logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure: ERROR
