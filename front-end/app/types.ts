export type RoadmapMeta = {
    id: string,
    name: string
    roadMapReferenceId: string
}

export type Roadmap = {
    id: string,
    obj: string
}

export type RoadmapDTO = {
    name: string,
    roadMap: string
}